<IfModule mod_rewrite.c>
    RewriteEngine On

    # Serve the app from /public without exposing it in the URL.
    RewriteCond %{REQUEST_URI} !(^|/)public(/|$) [NC]
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
