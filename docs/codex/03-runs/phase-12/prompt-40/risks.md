# Risks

- Prompt-40 finalizes the current guardian-payment route and policy boundaries only; it does not add any later donor or guest `payments.*` status/detail route family beyond the existing `/donate` checkout/status surfaces.
- Legacy management compatibility still depends on the existing non-portal account heuristics; this prompt intentionally closed the donor-profile leak only, not the entire legacy management identity model.
