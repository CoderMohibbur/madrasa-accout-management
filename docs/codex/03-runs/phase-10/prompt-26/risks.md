# Risks

- If prompt-29 through prompt-32 are treated as a simple schema/auth upgrade instead of a dark-launch plus classification-first cutover, the rollout can break login and portal eligibility before later phases start.
- If donor payment entry ships before the authoritative donor payable model is in place, public donation can recreate the same unsafe settlement ambiguity the analysis track is avoiding.
- If guardian informational and guardian protected phases are compressed together, the first guardian-facing release will carry unnecessary ownership-leak risk.
- If route/middleware finalization happens before Google and multi-role behavior are proven independently, eligibility-driven home behavior can regress into partial role-order redirects.
- If final rollout planning ignores the documented baseline auth/profile failures, prompt-43 can produce a false no-go or hide a real regression inside stale failures.
- If external admission handoff is treated as a harmless global CTA instead of a bounded surface, protected guardian or auth pages can regress into out-of-scope admission entry points.
