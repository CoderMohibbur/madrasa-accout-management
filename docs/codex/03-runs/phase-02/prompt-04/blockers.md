# Blockers

- No hard blocker prevents prompt-04 state-model analysis.
- Later prompts must not implement broader donor, guardian, or payment behavior on top of the current overloaded `email_verified_at` design without first respecting the mandatory separation rules captured here.
