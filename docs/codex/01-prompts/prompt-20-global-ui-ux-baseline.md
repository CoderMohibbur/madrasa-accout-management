Use the docs/codex-autopilot workflow from my project.

This task is PHASE_8_GLOBAL_UI_UX_BASELINE_ANALYSIS_ONLY.
Do not implement code yet.

Read:
1) resources/views/guardian/*
2) resources/views/donor/*
3) relevant auth/public/dashboard views
4) shared layouts/components/partials if present

Mandatory requirements:
- do not assume the existing UI pattern is the target standard
- choose one global-standard, modern, usability-first design direction
- all new and revised surfaces must follow one coherent pattern
- donor, guardian, public, auth, and multi-role surfaces must feel like one product family
- accessibility, responsiveness, readability, and clean interaction consistency are first-class requirements

Do only this:
1) audit the current UI for inconsistency, fragmentation, weak patterns, and UX problems
2) select one global-standard UI/UX baseline direction suitable for this project
3) define rules for:
   - page layout
   - spacing
   - typography
   - headings
   - button hierarchy
   - cards
   - lists
   - tables
   - forms and inputs
   - validation and error states
   - success states
   - empty states
   - loading states
   - no-access states
   - mobile responsiveness
   - accessibility expectations
4) identify which current UI parts should be preserved, normalized, replaced, or avoided
5) define the minimum shared design language that must be implemented before feature page UI work

Do not implement code.

End with:
- UI audit summary
- selected design direction
- unified UI rules
- preserve/normalize/replace/avoid list
- minimum shared design foundation
