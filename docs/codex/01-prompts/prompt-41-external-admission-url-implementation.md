Use the docs/codex-autopilot workflow from my project.

This task is PHASE_12_EXTERNAL_ADMISSION_URL_CONFIGURATION_IMPLEMENTATION.
Implement only the approved external admission URL configuration slice.

Before coding:
1) restate the exact approved slice
2) restate config ownership rules
3) restate fallback/validation requirements

Implement only:
- safe configuration support for external admission URL
- approved UI placements
- no hard-coded duplicate link spreading
- no admission-system expansion

End with:
- files changed
- external admission URL configuration implemented
- UI placements updated
- validation/fallback behavior notes
- next safe slice
